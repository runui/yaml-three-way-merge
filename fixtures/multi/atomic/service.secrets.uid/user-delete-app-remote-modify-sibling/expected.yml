services:
    app:
        image: busybox:latest
    sidecar:
        image: busybox:latest
        secrets:
            - source: base
              target: /run/config
              uid: remote-value
