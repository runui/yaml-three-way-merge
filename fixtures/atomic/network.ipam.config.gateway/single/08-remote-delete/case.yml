id: atomic/network.ipam.config.gateway/single/08-remote-delete
field: networks.default.ipam.config[].gateway
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
