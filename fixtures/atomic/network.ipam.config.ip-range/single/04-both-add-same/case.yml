id: atomic/network.ipam.config.ip-range/single/04-both-add-same
field: networks.default.ipam.config[].ip_range
class: atomic
scenario: 04-both-add-same
description: user changed the logical item relative to oldbase, so user intent wins
pair_semantics: none
