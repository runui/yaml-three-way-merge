services:
    app:
        oom_kill_disable: !reset []
---
services:
    app:
        oom_kill_disable: true
---
services:
    sidecar:
        oom_kill_disable: !reset []
---
services:
    sidecar:
        oom_kill_disable: true
