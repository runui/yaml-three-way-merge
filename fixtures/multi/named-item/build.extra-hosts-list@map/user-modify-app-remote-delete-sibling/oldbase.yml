services:
    app:
        build:
            extra_hosts:
                build-host-a: base
        image: busybox:latest
    sidecar:
        build:
            extra_hosts:
                build-host-b: base
        image: busybox:latest
