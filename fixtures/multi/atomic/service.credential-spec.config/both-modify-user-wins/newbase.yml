services:
    app:
        credential_spec:
            config: remote-value
        image: busybox:latest
    sidecar:
        credential_spec:
            config: remote-value
        image: busybox:latest
