services:
    app:
        deploy:
            endpoint_mode: remote-value
        image: busybox:latest
    sidecar:
        deploy:
            endpoint_mode: remote-value
        image: busybox:latest
