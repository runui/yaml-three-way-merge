services:
    app:
        deploy:
            update_config:
                failure_action: pause
        image: busybox:latest
    sidecar:
        deploy:
            update_config:
                failure_action: pause
        image: busybox:latest
