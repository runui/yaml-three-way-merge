services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - consistency: user-value
              source: /host
              target: /container
              type: bind
