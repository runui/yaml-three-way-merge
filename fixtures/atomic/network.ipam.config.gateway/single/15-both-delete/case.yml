id: atomic/network.ipam.config.gateway/single/15-both-delete
field: networks.default.ipam.config[].gateway
class: atomic
scenario: 15-both-delete
description: user changed the logical item relative to oldbase, so user intent wins
pair_semantics: none
