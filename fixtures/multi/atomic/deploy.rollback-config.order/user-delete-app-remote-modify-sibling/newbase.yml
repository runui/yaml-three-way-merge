services:
    app:
        deploy:
            rollback_config:
                order: start-first
        image: busybox:latest
    sidecar:
        deploy:
            rollback_config:
                order: stop-first
        image: busybox:latest
