id: atomic/deploy.rollback-config.failure-action/single/08-remote-delete
field: services.app.deploy.rollback_config.failure_action
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
