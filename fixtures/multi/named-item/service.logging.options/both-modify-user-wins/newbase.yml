services:
    app:
        image: busybox:latest
        logging:
            options:
                max-size: max-size-a-remote
    sidecar:
        image: busybox:latest
        logging:
            options:
                max-file: max-size-b-remote
