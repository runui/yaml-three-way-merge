services:
    app:
        build:
            secrets: !reset []
---
services:
    app:
        build:
            secrets:
                - mode: 2
                  source: base
                  target: /run/secrets/base
