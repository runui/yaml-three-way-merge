id: atomic/network.ipam.config.gateway/single/02-remote-add
field: networks.default.ipam.config[].gateway
class: atomic
scenario: 02-remote-add
description: user is untouched, so the logical item follows newbase
pair_semantics: none
