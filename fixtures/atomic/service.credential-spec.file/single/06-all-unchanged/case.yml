id: atomic/service.credential-spec.file/single/06-all-unchanged
field: services.app.credential_spec.file
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
