id: atomic/service.credential-spec.registry/single/01-all-absent
field: services.app.credential_spec.registry
class: atomic
scenario: 01-all-absent
description: user is untouched, so the logical item follows newbase
pair_semantics: none
