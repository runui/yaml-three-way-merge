services:
    app:
        configs: !reset []
---
services:
    app:
        configs:
            - mode: 292
              source: base_config
              target: /config
              uid: "1001"
