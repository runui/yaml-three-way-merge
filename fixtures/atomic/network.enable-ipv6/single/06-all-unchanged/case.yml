id: atomic/network.enable-ipv6/single/06-all-unchanged
field: networks.default.enable_ipv6
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
