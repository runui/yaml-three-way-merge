services:
    app:
        deploy:
            placement:
                constraints:
                    - node.labels.zone==remote-a
                    - node.role==manager-b
        image: busybox:latest
