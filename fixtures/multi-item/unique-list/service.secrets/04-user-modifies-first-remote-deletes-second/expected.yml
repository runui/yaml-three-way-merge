services:
    app:
        image: busybox:latest
        secrets:
            - source: user_secret_a
              target: /secret-a
            - source: base_secret_c
              target: /secret-c
