services:
    app:
        build:
            secrets: !reset []
---
services:
    app:
        build:
            secrets:
                - source: base_secret_a
                  target: /run/secrets/a
