id: atomic/service.credential-spec.file/single/08-remote-delete
field: services.app.credential_spec.file
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
