services:
    app:
        image: busybox:latest
        secrets:
            - source: user_secret_a
              target: /secret-a
            - source: remote_secret_b
              target: /secret-b
