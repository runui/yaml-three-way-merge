services:
    app:
        image: busybox:latest
        volumes:
            - consistency: remote-value
              source: /host
              target: /container
              type: bind
    sidecar:
        image: busybox:latest
        volumes:
            - consistency: remote-value
              source: /host
              target: /container
              type: bind
