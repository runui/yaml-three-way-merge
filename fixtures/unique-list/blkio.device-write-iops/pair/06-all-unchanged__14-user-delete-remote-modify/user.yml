services:
    app:
        blkio_config:
            device_write_iops: !reset []
---
services:
    app:
        blkio_config:
            device_write_iops:
                - path: /dev/a
                  rate: "100"
