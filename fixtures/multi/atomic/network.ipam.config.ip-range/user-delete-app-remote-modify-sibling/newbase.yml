networks:
    default:
        ipam:
            config:
                - ip_range: base-value
                  subnet: 10.0.0.0/24
    other:
        ipam:
            config:
                - ip_range: remote-value
                  subnet: 10.0.0.0/24
services:
    app:
        image: busybox:latest
