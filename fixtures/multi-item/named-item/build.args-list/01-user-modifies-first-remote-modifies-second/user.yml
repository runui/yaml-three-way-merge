services:
    app:
        build:
            args: !reset []
---
services:
    app:
        build:
            args:
                - ARG-a=user
                - ARG-b=base
                - ARG-c=base
