id: atomic/deploy.rollback-config.monitor/single/06-all-unchanged
field: services.app.deploy.rollback_config.monitor
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
