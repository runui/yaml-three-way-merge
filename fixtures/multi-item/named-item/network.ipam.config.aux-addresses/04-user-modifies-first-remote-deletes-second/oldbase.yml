networks:
    default:
        ipam:
            config:
                - aux_addresses:
                    host-a: aux-a-base
                    host-b: aux-b-base
                    host-b-c: aux-b-base
                  subnet: 10.0.0.0/24
services:
    app:
        image: busybox:latest
