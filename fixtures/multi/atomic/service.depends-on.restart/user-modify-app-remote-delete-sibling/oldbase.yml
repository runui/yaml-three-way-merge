services:
    app:
        depends_on:
            default:
                condition: service_started
                restart: false
        image: busybox:latest
    sidecar:
        depends_on:
            default:
                condition: service_started
                restart: false
        image: busybox:latest
