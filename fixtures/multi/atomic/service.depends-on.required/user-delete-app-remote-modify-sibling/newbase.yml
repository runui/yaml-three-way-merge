services:
    app:
        depends_on:
            default:
                required: false
        image: busybox:latest
    sidecar:
        depends_on:
            default:
                required: true
        image: busybox:latest
