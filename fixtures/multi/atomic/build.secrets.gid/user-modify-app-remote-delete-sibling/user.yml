services:
    app:
        build:
            secrets: !reset []
---
services:
    app:
        build:
            secrets:
                - gid: user-value
                  source: base
                  target: /run/secrets/base
