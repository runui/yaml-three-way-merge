services:
    app:
        deploy:
            rollback_config:
                failure_action: continue
        image: busybox:latest
    sidecar:
        deploy:
            rollback_config:
                failure_action: pause
        image: busybox:latest
