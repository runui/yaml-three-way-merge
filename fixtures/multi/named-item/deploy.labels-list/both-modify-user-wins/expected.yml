services:
    app:
        deploy:
            labels:
                - deploy.label-a=user
        image: busybox:latest
    sidecar:
        deploy:
            labels:
                - deploy.label-b=user
        image: busybox:latest
