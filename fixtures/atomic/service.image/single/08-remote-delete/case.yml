id: atomic/service.image/single/08-remote-delete
field: services.app.image
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
