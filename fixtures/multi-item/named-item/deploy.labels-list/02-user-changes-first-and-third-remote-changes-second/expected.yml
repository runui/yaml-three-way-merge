services:
    app:
        deploy:
            labels:
                - deploy.label-a=user
                - deploy.label-b=remote
                - deploy.label-c=user
        image: busybox:latest
