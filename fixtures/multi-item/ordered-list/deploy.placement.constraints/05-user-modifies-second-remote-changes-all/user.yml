services:
    app:
        deploy:
            placement:
                constraints: !reset []
---
services:
    app:
        deploy:
            placement:
                constraints:
                    - node.role==manager-a
                    - node.labels.zone==user-b
                    - node.role==manager-c
