services:
    app:
        networks:
            default:
                aliases: !reset []
---
services:
    app:
        networks:
            default:
                aliases:
                    - base-alias-a
                    - base-alias-b
                    - user-alias-c
