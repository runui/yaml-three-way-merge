networks:
    default:
        ipam:
            config: !reset []
---
networks:
    default:
        ipam:
            config:
                - gateway: user-value
                  subnet: 10.0.0.0/24
---
networks:
    other:
        ipam:
            config: !reset []
---
networks:
    other:
        ipam:
            config:
                - gateway: user-value
                  subnet: 10.0.0.0/24
