services:
    app:
        image: busybox:latest
        secrets:
            - source: base_secret_a
              target: /secret-a
            - source: base_secret_c
              target: /secret-c
