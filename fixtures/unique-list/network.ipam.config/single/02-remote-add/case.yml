id: unique-list/network.ipam.config/single/02-remote-add
field: networks.default.ipam.config
class: unique-list
scenario: 02-remote-add
description: user is untouched, so the logical item follows newbase
