services:
    app:
        device_cgroup_rules: !reset []
---
services:
    app:
        device_cgroup_rules:
            - c 1:5 rwm-a
---
services:
    sidecar:
        device_cgroup_rules: !reset []
---
services:
    sidecar:
        device_cgroup_rules:
            - c 1:5 rwm-b
