id: atomic/build.secrets.uid/single/06-all-unchanged
field: services.app.build.secrets[].uid
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
