services:
    app:
        credential_spec:
            registry: user-value
        image: busybox:latest
    sidecar:
        credential_spec:
            registry: user-value
        image: busybox:latest
