services:
    app:
        stdin_open: !reset []
---
services:
    app:
        stdin_open: true
---
services:
    sidecar:
        stdin_open: !reset []
---
services:
    sidecar:
        stdin_open: true
