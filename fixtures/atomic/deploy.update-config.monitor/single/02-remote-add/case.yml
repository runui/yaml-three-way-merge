id: atomic/deploy.update-config.monitor/single/02-remote-add
field: services.app.deploy.update_config.monitor
class: atomic
scenario: 02-remote-add
description: user is untouched, so the logical item follows newbase
pair_semantics: none
