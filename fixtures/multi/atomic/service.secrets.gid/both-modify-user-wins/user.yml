services:
    app:
        secrets: !reset []
---
services:
    app:
        secrets:
            - gid: user-value
              source: base
              target: /run/config
---
services:
    sidecar:
        secrets: !reset []
---
services:
    sidecar:
        secrets:
            - gid: user-value
              source: base
              target: /run/config
