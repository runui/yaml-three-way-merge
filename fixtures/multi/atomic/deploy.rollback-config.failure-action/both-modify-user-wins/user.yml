services:
    app:
        deploy:
            rollback_config:
                failure_action: !reset []
---
services:
    app:
        deploy:
            rollback_config:
                failure_action: pause
---
services:
    sidecar:
        deploy:
            rollback_config:
                failure_action: !reset []
---
services:
    sidecar:
        deploy:
            rollback_config:
                failure_action: pause
