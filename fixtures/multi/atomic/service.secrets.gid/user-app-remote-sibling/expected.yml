services:
    app:
        image: busybox:latest
        secrets:
            - gid: user-value
              source: base
              target: /run/config
    sidecar:
        image: busybox:latest
        secrets:
            - gid: remote-value
              source: base
              target: /run/config
