id: atomic/service.volumes.tmpfs.mode/single/08-remote-delete
field: services.app.volumes[].tmpfs.mode
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
