services:
    app:
        image: busybox:latest
        volumes:
            - consistency: base-value
              source: /host
              target: /container
              type: bind
    sidecar:
        image: busybox:latest
