services:
    app:
        build:
            ulimits: !reset {}
---
services:
    app:
        build:
            ulimits:
                nofile: nofile-a-base
                nproc: nofile-b-user
                nproc-c: nofile-b-base
