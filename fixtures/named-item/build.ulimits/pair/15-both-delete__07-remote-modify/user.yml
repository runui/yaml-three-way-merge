services:
    app:
        build:
            ulimits: !reset {}
---
services:
    app:
        build:
            ulimits:
                nproc:
                    hard: 200
                    soft: 100
