services:
    app:
        credential_spec:
            registry: remote-value
        image: busybox:latest
    sidecar:
        credential_spec:
            registry: remote-value
        image: busybox:latest
