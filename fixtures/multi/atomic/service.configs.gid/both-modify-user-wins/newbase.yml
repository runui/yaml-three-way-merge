services:
    app:
        configs:
            - gid: remote-value
              source: base
              target: /run/config
        image: busybox:latest
    sidecar:
        configs:
            - gid: remote-value
              source: base
              target: /run/config
        image: busybox:latest
