services:
    app:
        deploy:
            resources:
                reservations:
                    memory: base-value
        image: busybox:latest
    sidecar:
        deploy:
            resources:
                reservations:
                    memory: base-value
        image: busybox:latest
