services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - read_only: true
              source: /base
              target: /container
              type: bind
