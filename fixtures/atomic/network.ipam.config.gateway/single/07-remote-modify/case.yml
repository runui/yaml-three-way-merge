id: atomic/network.ipam.config.gateway/single/07-remote-modify
field: networks.default.ipam.config[].gateway
class: atomic
scenario: 07-remote-modify
description: user is untouched, so the logical item follows newbase
pair_semantics: none
