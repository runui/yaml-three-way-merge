services:
    app:
        deploy:
            update_config:
                max_failure_ratio: !reset []
---
services:
    app:
        deploy:
            update_config:
                max_failure_ratio: 0.2
---
services:
    sidecar:
        deploy:
            update_config:
                max_failure_ratio: !reset []
---
services:
    sidecar:
        deploy:
            update_config:
                max_failure_ratio: 0.2
