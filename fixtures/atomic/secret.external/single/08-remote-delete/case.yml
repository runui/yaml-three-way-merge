id: atomic/secret.external/single/08-remote-delete
field: secrets.default.external
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
