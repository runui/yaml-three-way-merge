services: !reset {}
---
services:
  app:
    deploy:
      resources:
        limits:
          devices:
            - capabilities:
                - gpu-a
              driver: nvidia
    image: busybox:latest
