services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - read_only: true
              source: /host
              target: /container
              type: bind
