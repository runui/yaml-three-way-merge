services:
    app:
        deploy:
            update_config:
                parallelism: !reset []
---
services:
    app:
        deploy:
            update_config:
                parallelism: 2
---
services:
    sidecar:
        deploy:
            update_config:
                parallelism: !reset []
---
services:
    sidecar:
        deploy:
            update_config:
                parallelism: 2
