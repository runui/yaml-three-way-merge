services:
    app:
        deploy:
            placement:
                constraints: !reset []
---
services:
    app:
        deploy:
            placement:
                constraints:
                    - node.role==manager-b
