services:
    app:
        deploy:
            placement:
                constraints: !reset []
---
services:
    app:
        deploy:
            placement:
                constraints:
                    - node.labels.zone==user-b
