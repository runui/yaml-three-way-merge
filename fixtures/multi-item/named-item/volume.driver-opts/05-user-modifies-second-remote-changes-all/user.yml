volumes:
    default:
        driver_opts: !reset {}
---
volumes:
    default:
        driver_opts:
            device: volume-option-b-user
            device-c: volume-option-b-base
            type: volume-option-a-base
