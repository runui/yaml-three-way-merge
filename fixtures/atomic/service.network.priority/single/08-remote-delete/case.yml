id: atomic/service.network.priority/single/08-remote-delete
field: services.app.networks.default.priority
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
