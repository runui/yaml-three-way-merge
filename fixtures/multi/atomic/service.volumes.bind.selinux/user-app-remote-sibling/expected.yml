services:
    app:
        image: busybox:latest
        volumes:
            - bind:
                selinux: user-value
              source: /host
              target: /container
              type: bind
    sidecar:
        image: busybox:latest
        volumes:
            - bind:
                selinux: remote-value
              source: /host
              target: /container
              type: bind
