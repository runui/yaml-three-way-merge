services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - bind:
                selinux: Z
              source: /host
              target: /container
              type: bind
---
services:
    sidecar:
        volumes: !reset []
---
services:
    sidecar:
        volumes:
            - bind:
                selinux: Z
              source: /host
              target: /container
              type: bind
