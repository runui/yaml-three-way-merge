services:
    app:
        build:
            dockerfile_inline: base-value
        image: busybox:latest
    sidecar:
        build:
            dockerfile_inline: base-value
        image: busybox:latest
