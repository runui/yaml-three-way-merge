id: unique-list/network.ipam.config/single/06-all-unchanged
field: networks.default.ipam.config
class: unique-list
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
