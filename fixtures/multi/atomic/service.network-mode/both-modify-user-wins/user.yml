services:
    app:
        network_mode: !reset []
---
services:
    app:
        network_mode: user-value
---
services:
    sidecar:
        network_mode: !reset []
---
services:
    sidecar:
        network_mode: user-value
