id: atomic/network.ipam.driver/single/06-all-unchanged
field: networks.default.ipam.driver
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
