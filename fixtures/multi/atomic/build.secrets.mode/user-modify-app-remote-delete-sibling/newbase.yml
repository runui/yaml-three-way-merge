services:
    app:
        build:
            secrets:
                - mode: 1
                  source: base
                  target: /run/secrets/base
        image: busybox:latest
    sidecar:
        image: busybox:latest
