services:
    app:
        blkio_config:
            device_write_bps: !reset []
---
services:
    app:
        blkio_config:
            device_write_bps:
                - path: /dev/a
                  rate: 21mb
                - path: /dev/b
                  rate: 111mb
