services:
    app:
        deploy:
            resources:
                reservations:
                    memory: !reset []
---
services:
    app:
        deploy:
            resources:
                reservations:
                    memory: user-value
