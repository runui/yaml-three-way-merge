id: atomic/service.network.ipv6-address/single/06-all-unchanged
field: services.app.networks.default.ipv6_address
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
