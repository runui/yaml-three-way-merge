services:
    app:
        build:
            secrets:
                - source: remote_secret_a
                  target: /run/secrets/a
                - source: user_secret_b
                  target: /run/secrets/b
                - source: remote_secret_c
                  target: /run/secrets/c
        image: busybox:latest
