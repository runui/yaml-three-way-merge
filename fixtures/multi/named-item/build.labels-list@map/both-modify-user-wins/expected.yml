services:
    app:
        build:
            labels:
                build.label-a: user
        image: busybox:latest
    sidecar:
        build:
            labels:
                build.label-b: user
        image: busybox:latest
