services:
    app:
        deploy:
            update_config:
                parallelism: 1
        image: busybox:latest
    sidecar:
        deploy:
            update_config:
                parallelism: 3
        image: busybox:latest
