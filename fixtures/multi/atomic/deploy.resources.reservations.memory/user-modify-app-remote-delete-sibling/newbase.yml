services:
    app:
        deploy:
            resources:
                reservations:
                    memory: base-value
        image: busybox:latest
    sidecar:
        image: busybox:latest
