id: ordered-list/include.path/single/06-all-unchanged
field: include[].path
class: ordered-list
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: whole-list
