services:
    app:
        deploy:
            resources:
                limits:
                    memory: user-value
        image: busybox:latest
    sidecar:
        deploy:
            resources:
                limits:
                    memory: remote-value
        image: busybox:latest
