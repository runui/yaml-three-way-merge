services:
    app:
        configs:
            - source: base
              target: /run/config
              uid: base-value
        image: busybox:latest
    sidecar:
        configs:
            - source: base
              target: /run/config
              uid: remote-value
        image: busybox:latest
