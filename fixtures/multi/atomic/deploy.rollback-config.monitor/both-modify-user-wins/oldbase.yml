services:
    app:
        deploy:
            rollback_config:
                monitor: 1s
        image: busybox:latest
    sidecar:
        deploy:
            rollback_config:
                monitor: 1s
        image: busybox:latest
