services:
    app:
        configs:
            - mode: 1
              source: base
              target: /run/config
        image: busybox:latest
    sidecar:
        configs:
            - mode: 1
              source: base
              target: /run/config
        image: busybox:latest
