id: atomic/service.credential-spec.registry/single/08-remote-delete
field: services.app.credential_spec.registry
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
