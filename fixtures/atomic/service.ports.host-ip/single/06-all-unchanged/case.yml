id: atomic/service.ports.host-ip/single/06-all-unchanged
field: services.app.ports[].host_ip
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
