id: atomic/service.oom-kill-disable/single/06-all-unchanged
field: services.app.oom_kill_disable
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
