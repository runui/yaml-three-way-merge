services:
    app:
        sysctls: !reset {}
---
services:
    app:
        sysctls:
            net.core.value-a: user
---
services:
    sidecar:
        sysctls: !reset {}
---
services:
    sidecar:
        sysctls:
            net.core.value-b: user
