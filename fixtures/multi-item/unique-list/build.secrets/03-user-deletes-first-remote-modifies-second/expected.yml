services:
    app:
        build:
            secrets:
                - source: remote_secret_b
                  target: /run/secrets/b
                - source: base_secret_c
                  target: /run/secrets/c
        image: busybox:latest
