services:
    app:
        image: busybox:latest
volumes:
    default:
        driver_opts:
            type: volume-option-a-remote
    other:
        driver_opts:
            device: volume-option-b-remote
