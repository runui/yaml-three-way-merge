services:
    app:
        credential_spec:
            registry: !reset []
---
services:
    app:
        credential_spec:
            registry: user-value
---
services:
    sidecar:
        credential_spec:
            registry: !reset []
---
services:
    sidecar:
        credential_spec:
            registry: user-value
