services:
    app:
        deploy:
            placement:
                constraints:
                    - node.role==manager-a
        image: busybox:latest
    sidecar:
        image: busybox:latest
