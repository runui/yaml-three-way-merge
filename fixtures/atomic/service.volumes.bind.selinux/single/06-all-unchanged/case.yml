id: atomic/service.volumes.bind.selinux/single/06-all-unchanged
field: services.app.volumes[].bind.selinux
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
