services:
    app:
        deploy:
            rollback_config:
                parallelism: !reset []
---
services:
    app:
        deploy:
            rollback_config:
                parallelism: 2
