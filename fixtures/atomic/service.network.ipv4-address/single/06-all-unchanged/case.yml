id: atomic/service.network.ipv4-address/single/06-all-unchanged
field: services.app.networks.default.ipv4_address
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
