services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - source: data
              target: /container
              type: volume
---
services:
    sidecar:
        volumes: !reset []
---
services:
    sidecar:
        volumes:
            - source: data
              target: /container
              type: volume
