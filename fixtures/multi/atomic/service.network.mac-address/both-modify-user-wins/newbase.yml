services:
    app:
        image: busybox:latest
        networks:
            default:
                mac_address: remote-value
    sidecar:
        image: busybox:latest
        networks:
            default:
                mac_address: remote-value
