services:
    app:
        image: busybox:latest
        secrets:
            - source: base_secret_a
              target: /secret-a
            - source: remote_secret_b
              target: /secret-b
            - source: base_secret_c
              target: /secret-c
