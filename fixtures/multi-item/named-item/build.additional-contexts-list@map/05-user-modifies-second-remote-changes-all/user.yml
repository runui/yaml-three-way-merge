services:
    app:
        build:
            additional_contexts: !reset {}
---
services:
    app:
        build:
            additional_contexts:
                context-a: base
                context-b: user
                context-c: base
