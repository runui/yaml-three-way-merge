services:
    app:
        configs:
            - gid: base-value
              source: base
              target: /run/config
        image: busybox:latest
    sidecar:
        configs:
            - gid: base-value
              source: base
              target: /run/config
        image: busybox:latest
