services:
    app:
        depends_on:
            default:
                condition: service_started
                required: false
        image: busybox:latest
    sidecar:
        depends_on:
            default:
                condition: service_started
                required: true
        image: busybox:latest
