networks:
    default:
        ipam:
            config:
                - aux_addresses:
                    host-a: aux-a-base
                    host-b: aux-b-remote
                    host-b-c: aux-b-user
                  subnet: 10.0.0.0/24
services:
    app:
        image: busybox:latest
