services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - read_only: true
              source: /user
              target: /container
              type: bind
