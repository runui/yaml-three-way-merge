services:
    app:
        image: busybox:latest
        volumes:
            - bind:
                propagation: user-value
              source: /host
              target: /container
              type: bind
