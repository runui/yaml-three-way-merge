services:
    app:
        build:
            context: !reset []
---
services:
    app:
        build:
            context: user-value
---
services:
    sidecar:
        build:
            context: !reset []
---
services:
    sidecar:
        build:
            context: user-value
