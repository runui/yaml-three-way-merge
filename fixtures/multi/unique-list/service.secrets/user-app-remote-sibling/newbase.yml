services:
    app:
        image: busybox:latest
        secrets:
            - source: base_secret_a
              target: /secret-a
    sidecar:
        image: busybox:latest
        secrets:
            - source: remote_secret_b
              target: /secret-b
