id: atomic/service.volumes.bind.selinux/single/01-all-absent
field: services.app.volumes[].bind.selinux
class: atomic
scenario: 01-all-absent
description: user is untouched, so the logical item follows newbase
pair_semantics: none
