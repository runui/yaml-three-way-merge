services:
    app:
        configs:
            - gid: user-value
              source: base
              target: /run/config
        image: busybox:latest
    sidecar:
        configs:
            - gid: user-value
              source: base
              target: /run/config
        image: busybox:latest
