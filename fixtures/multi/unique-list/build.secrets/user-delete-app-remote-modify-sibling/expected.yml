services:
    app:
        image: busybox:latest
    sidecar:
        build:
            secrets:
                - source: remote_secret_b
                  target: /run/secrets/b
        image: busybox:latest
