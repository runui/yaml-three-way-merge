services:
    app:
        blkio_config:
            device_write_bps:
                - path: /dev/a
                  rate: 21mb
        image: busybox:latest
    sidecar:
        image: busybox:latest
