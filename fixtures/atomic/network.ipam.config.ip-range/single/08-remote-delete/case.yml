id: atomic/network.ipam.config.ip-range/single/08-remote-delete
field: networks.default.ipam.config[].ip_range
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
