services:
    app:
        build:
            secrets:
                - source: base
                  target: /run/secrets/base
                  uid: base-value
        image: busybox:latest
    sidecar:
        image: busybox:latest
