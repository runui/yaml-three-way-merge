services:
    app:
        configs:
            - source: base_config_a
              target: /config-a
        image: busybox:latest
    sidecar:
        configs:
            - source: base_config_b
              target: /config-b
        image: busybox:latest
