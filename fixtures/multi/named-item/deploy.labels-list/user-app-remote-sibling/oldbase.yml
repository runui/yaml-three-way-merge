services:
    app:
        deploy:
            labels:
                - deploy.label-a=base
        image: busybox:latest
    sidecar:
        deploy:
            labels:
                - deploy.label-b=base
        image: busybox:latest
