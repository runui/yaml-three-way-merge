services:
    app:
        build:
            ssh: !reset []
---
services:
    app:
        build:
            ssh:
                - user-a
---
services:
    sidecar:
        build:
            ssh: !reset []
---
services:
    sidecar:
        build:
            ssh:
                - user-b
