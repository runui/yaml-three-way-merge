services:
    app:
        deploy:
            update_config:
                parallelism: 2
        image: busybox:latest
    sidecar:
        deploy:
            update_config:
                parallelism: 3
        image: busybox:latest
