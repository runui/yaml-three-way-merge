services:
    app:
        deploy:
            resources:
                limits:
                    devices: !reset []
---
services:
    app:
        deploy:
            resources:
                limits:
                    devices:
                        - capabilities:
                            - gpu
                          driver: nvidia
                          options:
                            a: device-option-a-user
                            b: device-option-b-base
                            b-c: device-option-b-user
