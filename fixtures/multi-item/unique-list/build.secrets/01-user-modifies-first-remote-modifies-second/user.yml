services:
    app:
        build:
            secrets: !reset []
---
services:
    app:
        build:
            secrets:
                - source: user_secret_a
                  target: /run/secrets/a
                - source: base_secret_b
                  target: /run/secrets/b
                - source: base_secret_c
                  target: /run/secrets/c
