services:
    app:
        logging:
            options: !reset {}
---
services:
    app:
        logging:
            options:
                max-size: max-size-a-user
---
services:
    sidecar:
        logging:
            options: !reset {}
---
services:
    sidecar:
        logging:
            options:
                max-file: max-size-b-user
