services:
    app:
        build:
            ulimits: !reset {}
---
services:
    app:
        build:
            ulimits:
                nproc:
                    hard: 300
                    soft: 200
