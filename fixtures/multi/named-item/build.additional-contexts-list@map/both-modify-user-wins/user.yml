services:
    app:
        build:
            additional_contexts: !reset {}
---
services:
    app:
        build:
            additional_contexts:
                context-a: user
---
services:
    sidecar:
        build:
            additional_contexts: !reset {}
---
services:
    sidecar:
        build:
            additional_contexts:
                context-b: user
