services:
    app:
        build:
            ulimits: !reset {}
---
services:
    app:
        build:
            ulimits:
                nproc: nofile-b-base
                nproc-c: nofile-b-base
