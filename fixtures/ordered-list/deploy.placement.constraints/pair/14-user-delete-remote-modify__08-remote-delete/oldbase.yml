services:
    app:
        deploy:
            placement:
                constraints:
                    - node.role==manager-a
                    - node.role==manager-b
        image: busybox:latest
