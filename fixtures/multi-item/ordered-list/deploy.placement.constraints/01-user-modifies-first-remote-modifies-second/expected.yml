services:
    app:
        deploy:
            placement:
                constraints:
                    - node.labels.zone==user-a
                    - node.role==manager-b
                    - node.role==manager-c
        image: busybox:latest
