services:
    app:
        deploy:
            resources:
                limits:
                    generic_resources:
                        - discrete_resource_spec:
                            kind: remote-value
                            value: 1
        image: busybox:latest
