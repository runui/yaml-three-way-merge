id: atomic/include.project-directory/single/08-remote-delete
field: include[].project_directory
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
