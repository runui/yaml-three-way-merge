services:
    app:
        depends_on:
            default:
                condition: service_started
        image: busybox:latest
    sidecar:
        depends_on:
            default:
                condition: service_started
                required: true
        image: busybox:latest
