services:
    app:
        deploy:
            update_config:
                max_failure_ratio: !reset []
---
services:
    app:
        deploy:
            update_config:
                max_failure_ratio: 0.2
