services:
    app:
        deploy:
            placement:
                constraints: !reset []
---
services:
    app:
        deploy:
            placement:
                constraints:
                    - node.labels.zone==user-a
                    - node.role==manager-b
                    - node.role==manager-c
