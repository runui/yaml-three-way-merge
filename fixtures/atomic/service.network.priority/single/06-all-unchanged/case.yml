id: atomic/service.network.priority/single/06-all-unchanged
field: services.app.networks.default.priority
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
