services:
    app:
        configs:
            - source: user_config_a
              target: /config-a
        image: busybox:latest
    sidecar:
        configs:
            - source: user_config_b
              target: /config-b
        image: busybox:latest
