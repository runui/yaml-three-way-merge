services:
    app:
        command: !reset []
---
services:
    app:
        command: echo user-a
---
services:
    sidecar:
        command: !reset []
---
services:
    sidecar:
        command: echo user-a
