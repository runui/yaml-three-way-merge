id: atomic/config.template-driver/single/08-remote-delete
field: configs.default.template_driver
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
