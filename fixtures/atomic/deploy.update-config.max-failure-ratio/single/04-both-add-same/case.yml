id: atomic/deploy.update-config.max-failure-ratio/single/04-both-add-same
field: services.app.deploy.update_config.max_failure_ratio
class: atomic
scenario: 04-both-add-same
description: user changed the logical item relative to oldbase, so user intent wins
pair_semantics: none
