services:
    app:
        image: busybox:latest
        volumes:
            - bind:
                selinux: base-value
              source: /host
              target: /container
              type: bind
    sidecar:
        image: busybox:latest
        volumes:
            - bind:
                selinux: base-value
              source: /host
              target: /container
              type: bind
