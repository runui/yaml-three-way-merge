id: atomic/service.credential-spec.registry/single/10-user-delete
field: services.app.credential_spec.registry
class: atomic
scenario: 10-user-delete
description: user changed the logical item relative to oldbase, so user intent wins
pair_semantics: none
