services:
    app:
        credential_spec:
            registry: base-value
        image: busybox:latest
    sidecar:
        credential_spec:
            registry: base-value
        image: busybox:latest
