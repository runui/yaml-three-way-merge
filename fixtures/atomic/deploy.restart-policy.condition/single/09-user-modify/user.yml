services:
    app:
        deploy:
            restart_policy:
                condition: !reset []
---
services:
    app:
        deploy:
            restart_policy:
                condition: on-failure
