services:
    app:
        image: busybox:latest
    sidecar:
        image: busybox:latest
        volumes:
            - bind:
                propagation: remote-value
              source: /host
              target: /container
              type: bind
