id: atomic/deploy.update-config.delay/single/06-all-unchanged
field: services.app.deploy.update_config.delay
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
