services:
    app:
        image: busybox:latest
        volumes:
            - bind:
                create_host_path: true
              source: /host
              target: /container
              type: bind
