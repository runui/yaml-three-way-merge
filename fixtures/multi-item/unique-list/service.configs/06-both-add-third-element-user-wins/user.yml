services:
    app:
        configs: !reset []
---
services:
    app:
        configs:
            - source: base_config_a
              target: /config-a
            - source: base_config_b
              target: /config-b
            - source: user_config_c
              target: /config-c
