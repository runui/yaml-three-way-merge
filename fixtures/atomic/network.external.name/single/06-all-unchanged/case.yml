id: atomic/network.external.name/single/06-all-unchanged
field: networks.default.external.name
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
