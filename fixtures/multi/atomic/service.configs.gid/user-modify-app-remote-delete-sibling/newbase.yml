services:
    app:
        configs:
            - gid: base-value
              source: base
              target: /run/config
        image: busybox:latest
    sidecar:
        image: busybox:latest
