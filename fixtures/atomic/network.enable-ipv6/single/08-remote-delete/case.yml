id: atomic/network.enable-ipv6/single/08-remote-delete
field: networks.default.enable_ipv6
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
