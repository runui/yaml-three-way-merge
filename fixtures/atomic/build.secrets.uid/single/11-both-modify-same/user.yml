services:
    app:
        build:
            secrets: !reset []
---
services:
    app:
        build:
            secrets:
                - source: base
                  target: /run/secrets/base
                  uid: user-value
