services:
    app:
        deploy:
            resources:
                limits:
                    memory: !reset []
---
services:
    app:
        deploy:
            resources:
                limits:
                    memory: user-value
---
services:
    sidecar:
        deploy:
            resources:
                limits:
                    memory: !reset []
---
services:
    sidecar:
        deploy:
            resources:
                limits:
                    memory: user-value
