services:
    app:
        image: busybox:latest
        volumes:
            - bind:
                selinux: Z
              source: /host
              target: /container
              type: bind
    sidecar:
        image: busybox:latest
        volumes:
            - bind:
                selinux: Z
              source: /host
              target: /container
              type: bind
