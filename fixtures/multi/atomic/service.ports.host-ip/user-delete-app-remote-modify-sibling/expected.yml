services:
    app:
        image: busybox:latest
    sidecar:
        image: busybox:latest
        ports:
            - host_ip: remote-value
              protocol: tcp
              published: "8080"
              target: 80
