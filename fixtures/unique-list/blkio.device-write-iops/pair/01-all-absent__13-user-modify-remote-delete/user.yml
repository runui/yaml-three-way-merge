services:
    app:
        blkio_config:
            device_write_iops: !reset []
---
services:
    app:
        blkio_config:
            device_write_iops:
                - path: /dev/b
                  rate: "12100"
