id: atomic/service.ports.host-ip/single/08-remote-delete
field: services.app.ports[].host_ip
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
