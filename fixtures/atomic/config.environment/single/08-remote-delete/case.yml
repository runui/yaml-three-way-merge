id: atomic/config.environment/single/08-remote-delete
field: configs.default.environment
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
