services:
    app:
        image: busybox:latest
volumes:
    default:
        driver_opts:
            device: volume-option-b-user
            device-c: volume-option-b-remote
            type: volume-option-a-remote
