id: unique-list/network.ipam.config/single/10-user-delete
field: networks.default.ipam.config
class: unique-list
scenario: 10-user-delete
description: user changed the logical item relative to oldbase, so user intent wins
