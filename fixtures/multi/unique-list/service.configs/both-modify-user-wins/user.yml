services:
    app:
        configs: !reset []
---
services:
    app:
        configs:
            - source: user_config_a
              target: /config-a
---
services:
    sidecar:
        configs: !reset []
---
services:
    sidecar:
        configs:
            - source: user_config_b
              target: /config-b
