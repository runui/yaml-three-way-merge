id: atomic/service.credential-spec.config/single/02-remote-add
field: services.app.credential_spec.config
class: atomic
scenario: 02-remote-add
description: user is untouched, so the logical item follows newbase
pair_semantics: none
