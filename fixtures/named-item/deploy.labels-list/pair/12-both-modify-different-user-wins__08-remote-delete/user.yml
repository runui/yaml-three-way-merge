services:
    app:
        deploy:
            labels: !reset []
---
services:
    app:
        deploy:
            labels:
                - deploy.label-a=user
                - deploy.label-b=base
