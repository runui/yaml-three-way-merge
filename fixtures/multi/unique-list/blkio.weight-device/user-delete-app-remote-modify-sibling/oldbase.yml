services:
    app:
        blkio_config:
            weight_device:
                - path: /dev/a
                  weight: 100
        image: busybox:latest
    sidecar:
        blkio_config:
            weight_device:
                - path: /dev/b
                  weight: 400
        image: busybox:latest
