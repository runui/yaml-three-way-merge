id: atomic/deploy.update-config.max-failure-ratio/single/06-all-unchanged
field: services.app.deploy.update_config.max_failure_ratio
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
