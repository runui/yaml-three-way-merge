services:
    app:
        deploy:
            update_config:
                failure_action: rollback
        image: busybox:latest
    sidecar:
        deploy:
            update_config:
                failure_action: rollback
        image: busybox:latest
