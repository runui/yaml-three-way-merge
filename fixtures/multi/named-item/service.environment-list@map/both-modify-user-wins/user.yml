services:
    app:
        environment: !reset {}
---
services:
    app:
        environment:
            ENV-a: user
---
services:
    sidecar:
        environment: !reset {}
---
services:
    sidecar:
        environment:
            ENV-b: user
