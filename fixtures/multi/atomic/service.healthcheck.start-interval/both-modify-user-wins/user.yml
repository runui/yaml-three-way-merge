services:
    app:
        healthcheck:
            start_interval: !reset []
---
services:
    app:
        healthcheck:
            start_interval: 2s
---
services:
    sidecar:
        healthcheck:
            start_interval: !reset []
---
services:
    sidecar:
        healthcheck:
            start_interval: 2s
