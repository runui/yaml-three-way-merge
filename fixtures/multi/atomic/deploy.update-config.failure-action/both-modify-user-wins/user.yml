services:
    app:
        deploy:
            update_config:
                failure_action: !reset []
---
services:
    app:
        deploy:
            update_config:
                failure_action: rollback
---
services:
    sidecar:
        deploy:
            update_config:
                failure_action: !reset []
---
services:
    sidecar:
        deploy:
            update_config:
                failure_action: rollback
