services:
    app:
        deploy:
            resources:
                limits:
                    memory: base-value
        image: busybox:latest
    sidecar:
        deploy:
            resources:
                limits:
                    memory: remote-value
        image: busybox:latest
