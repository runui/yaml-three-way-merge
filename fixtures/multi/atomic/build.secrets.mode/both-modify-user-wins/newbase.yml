services:
    app:
        build:
            secrets:
                - mode: 3
                  source: base
                  target: /run/secrets/base
        image: busybox:latest
    sidecar:
        build:
            secrets:
                - mode: 3
                  source: base
                  target: /run/secrets/base
        image: busybox:latest
