services:
    app:
        deploy:
            placement:
                preferences: !reset []
---
services:
    app:
        deploy:
            placement:
                preferences:
                    - spread: node.labels.user-a
                    - spread: node.labels.user-b
