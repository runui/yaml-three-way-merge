services:
    app:
        ports: !reset []
---
services:
    app:
        ports:
            - mode: ingress
              protocol: tcp
              published: "8080"
              target: 80
---
services:
    sidecar:
        ports: !reset []
---
services:
    sidecar:
        ports:
            - mode: ingress
              protocol: tcp
              published: "8080"
              target: 80
