id: atomic/service.volumes.bind.selinux/single/02-remote-add
field: services.app.volumes[].bind.selinux
class: atomic
scenario: 02-remote-add
description: user is untouched, so the logical item follows newbase
pair_semantics: none
