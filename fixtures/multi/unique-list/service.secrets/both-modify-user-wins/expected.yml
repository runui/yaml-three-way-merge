services:
    app:
        image: busybox:latest
        secrets:
            - source: user_secret_a
              target: /secret-a
    sidecar:
        image: busybox:latest
        secrets:
            - source: user_secret_b
              target: /secret-b
