id: atomic/service.volumes.bind.selinux/single/03-user-add
field: services.app.volumes[].bind.selinux
class: atomic
scenario: 03-user-add
description: user changed the logical item relative to oldbase, so user intent wins
pair_semantics: none
