services:
    app:
        depends_on:
            default:
                required: true
        image: busybox:latest
    sidecar:
        depends_on:
            default:
                required: true
        image: busybox:latest
