services:
    app:
        image: busybox:latest
        volumes:
            - bind:
                selinux: user-value
              source: /host
              target: /container
              type: bind
