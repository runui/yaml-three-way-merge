id: unique-list/network.ipam.config/single/08-remote-delete
field: networks.default.ipam.config
class: unique-list
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: logical-items
