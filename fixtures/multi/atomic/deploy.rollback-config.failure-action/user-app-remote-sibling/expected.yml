services:
    app:
        deploy:
            rollback_config:
                failure_action: rollback
        image: busybox:latest
    sidecar:
        deploy:
            rollback_config:
                failure_action: pause
        image: busybox:latest
