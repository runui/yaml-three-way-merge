services:
    app:
        deploy:
            rollback_config:
                parallelism: 1
        image: busybox:latest
    sidecar:
        deploy:
            rollback_config:
                parallelism: 3
        image: busybox:latest
