id: atomic/build.dockerfile-inline/single/08-remote-delete
field: services.app.build.dockerfile_inline
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
