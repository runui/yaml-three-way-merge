id: atomic/config.external/single/08-remote-delete
field: configs.default.external
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
