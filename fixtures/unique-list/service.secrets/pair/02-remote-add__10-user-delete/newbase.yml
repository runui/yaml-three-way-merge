services:
    app:
        image: busybox:latest
        secrets:
            - source: remote_secret_a
              target: /secret-a
            - source: base_secret_b
              target: /secret-b
