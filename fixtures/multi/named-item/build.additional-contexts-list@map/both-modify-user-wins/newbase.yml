services:
    app:
        build:
            additional_contexts:
                context-a: remote
        image: busybox:latest
    sidecar:
        build:
            additional_contexts:
                context-b: remote
        image: busybox:latest
