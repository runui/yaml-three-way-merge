id: ordered-list/include.path/single/08-remote-delete
field: include[].path
class: ordered-list
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: whole-list
