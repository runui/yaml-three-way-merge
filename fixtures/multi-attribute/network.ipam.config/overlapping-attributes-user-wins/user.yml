networks:
    default:
        ipam:
            config: !reset []
---
networks:
    default:
        ipam:
            config:
                - gateway: 10.0.0.2
                  ip_range: 10.0.0.0/26
                  subnet: 10.0.0.0/24
