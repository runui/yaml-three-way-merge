services:
    app:
        image: busybox:latest
        secrets:
            - source: base
              target: /run/config
              uid: base-value
    sidecar:
        image: busybox:latest
        secrets:
            - source: base
              target: /run/config
              uid: remote-value
