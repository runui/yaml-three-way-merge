services:
    app:
        deploy:
            labels:
                deploy.label-a: base
        image: busybox:latest
    sidecar:
        deploy:
            labels:
                deploy.label-b: remote
        image: busybox:latest
