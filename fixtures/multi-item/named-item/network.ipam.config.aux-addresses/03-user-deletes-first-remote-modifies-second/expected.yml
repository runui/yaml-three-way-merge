networks:
    default:
        ipam:
            config:
                - aux_addresses:
                    host-b: aux-b-remote
                    host-b-c: aux-b-base
                  subnet: 10.0.0.0/24
services:
    app:
        image: busybox:latest
