services:
    app:
        deploy:
            labels: !reset []
---
services:
    app:
        deploy:
            labels:
                - deploy.label-b=base
                - deploy.label-c=base
