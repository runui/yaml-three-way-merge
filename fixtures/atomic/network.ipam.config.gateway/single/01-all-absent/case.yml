id: atomic/network.ipam.config.gateway/single/01-all-absent
field: networks.default.ipam.config[].gateway
class: atomic
scenario: 01-all-absent
description: user is untouched, so the logical item follows newbase
pair_semantics: none
