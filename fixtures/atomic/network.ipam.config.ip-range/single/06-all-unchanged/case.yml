id: atomic/network.ipam.config.ip-range/single/06-all-unchanged
field: networks.default.ipam.config[].ip_range
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
