services:
    app:
        build:
            no_cache: !reset []
---
services:
    app:
        build:
            no_cache: true
---
services:
    sidecar:
        build:
            no_cache: !reset []
---
services:
    sidecar:
        build:
            no_cache: true
