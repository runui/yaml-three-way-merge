services:
    app:
        build:
            secrets: !reset []
---
services:
    app:
        build:
            secrets:
                - mode: 2
                  source: base
                  target: /run/secrets/base
---
services:
    sidecar:
        build:
            secrets: !reset []
---
services:
    sidecar:
        build:
            secrets:
                - mode: 2
                  source: base
                  target: /run/secrets/base
