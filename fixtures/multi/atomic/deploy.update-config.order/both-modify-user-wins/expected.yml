services:
    app:
        deploy:
            update_config:
                order: stop-first
        image: busybox:latest
    sidecar:
        deploy:
            update_config:
                order: stop-first
        image: busybox:latest
