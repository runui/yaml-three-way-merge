services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - bind:
                selinux: Z
              source: /host
              target: /container
              type: bind
