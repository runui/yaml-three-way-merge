services:
    app:
        deploy:
            resources:
                limits:
                    memory: !reset []
---
services:
    app:
        deploy:
            resources:
                limits:
                    memory: user-value
