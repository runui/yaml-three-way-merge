id: atomic/service.network.ipv4-address/single/08-remote-delete
field: services.app.networks.default.ipv4_address
class: atomic
scenario: 08-remote-delete
description: user is untouched, so the logical item follows newbase
pair_semantics: none
