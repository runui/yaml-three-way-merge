services:
    app:
        image: busybox:latest
        ports:
            - host_ip: base-value
              protocol: tcp
              published: "8080"
              target: 80
    sidecar:
        image: busybox:latest
        ports:
            - host_ip: remote-value
              protocol: tcp
              published: "8080"
              target: 80
