services:
    app:
        deploy:
            resources:
                reservations:
                    memory: remote-value
        image: busybox:latest
    sidecar:
        deploy:
            resources:
                reservations:
                    memory: remote-value
        image: busybox:latest
