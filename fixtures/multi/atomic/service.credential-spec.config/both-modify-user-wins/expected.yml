services:
    app:
        credential_spec:
            config: user-value
        image: busybox:latest
    sidecar:
        credential_spec:
            config: user-value
        image: busybox:latest
