services:
    app:
        image: busybox:latest
        logging:
            options:
                max-file: max-size-b-remote
                max-file-c: max-size-b-remote
                max-size: max-size-a-base
