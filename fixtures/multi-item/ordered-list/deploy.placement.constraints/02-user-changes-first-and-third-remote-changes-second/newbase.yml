services:
    app:
        deploy:
            placement:
                constraints:
                    - node.role==manager-a
                    - node.labels.zone==remote-b
                    - node.role==manager-c
        image: busybox:latest
