services:
    app:
        volumes: !reset []
---
services:
    app:
        volumes:
            - source: /host
              target: /container
              type: bind
              volume:
                nocopy: true
