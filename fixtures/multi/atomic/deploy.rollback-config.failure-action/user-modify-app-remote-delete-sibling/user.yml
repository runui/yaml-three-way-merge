services:
    app:
        deploy:
            rollback_config:
                failure_action: !reset []
---
services:
    app:
        deploy:
            rollback_config:
                failure_action: rollback
