services:
    app:
        deploy:
            labels: !reset []
---
services:
    app:
        deploy:
            labels:
                - deploy.label-a=base
                - deploy.label-b=base
                - deploy.label-c=user
