services:
    app:
        build:
            additional_contexts: !reset []
---
services:
    app:
        build:
            additional_contexts:
                - context-b=base
                - context-c=base
