services:
    app:
        deploy:
            resources:
                reservations:
                    memory: user-value
        image: busybox:latest
    sidecar:
        deploy:
            resources:
                reservations:
                    memory: user-value
        image: busybox:latest
