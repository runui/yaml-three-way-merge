services:
    app:
        networks:
            default:
                aliases: !reset []
---
services:
    app:
        networks:
            default:
                aliases:
                    - user-alias-a
                    - base-alias-b
                    - user-alias-c
