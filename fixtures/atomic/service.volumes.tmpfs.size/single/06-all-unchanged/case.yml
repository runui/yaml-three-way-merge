id: atomic/service.volumes.tmpfs.size/single/06-all-unchanged
field: services.app.volumes[].tmpfs.size
class: atomic
scenario: 06-all-unchanged
description: user is untouched, so the logical item follows newbase
pair_semantics: none
