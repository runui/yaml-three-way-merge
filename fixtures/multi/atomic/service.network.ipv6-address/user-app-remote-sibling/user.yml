services:
    app:
        networks:
            default:
                ipv6_address: !reset []
---
services:
    app:
        networks:
            default:
                ipv6_address: user-value
