services:
    app:
        deploy:
            rollback_config:
                parallelism: 2
        image: busybox:latest
    sidecar:
        deploy:
            rollback_config:
                parallelism: 3
        image: busybox:latest
