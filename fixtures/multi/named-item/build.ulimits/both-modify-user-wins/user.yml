services:
    app:
        build:
            ulimits: !reset {}
---
services:
    app:
        build:
            ulimits:
                nofile: nofile-a-user
---
services:
    sidecar:
        build:
            ulimits: !reset {}
---
services:
    sidecar:
        build:
            ulimits:
                nproc: nofile-b-user
