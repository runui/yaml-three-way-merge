services:
    app:
        deploy:
            placement:
                preferences:
                    - spread: node.labels.user-a
        image: busybox:latest
    sidecar:
        image: busybox:latest
